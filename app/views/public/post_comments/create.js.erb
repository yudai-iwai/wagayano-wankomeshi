$('#comments_area').html("<%= j(render 'public/post_comments/comment', post_recipe: @post_recipe, post_comment: @post_comment, post_comments: @post_comments) %>");
$('#comment_count').html("<%= @post_recipe.post_comments.count %>")
$("textarea").val('');
if ($('#comments_error').length) {
   $('#comments_error').remove();
}