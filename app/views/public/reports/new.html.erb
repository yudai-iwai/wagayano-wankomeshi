<div class="container">
  <div class="row">
    <div class="col-12">
      <div style="text-align:center; font-size:70px;" class="mt-5 mb-5">
        <h1 class="bg-danger">通　　報</h1>
      </div>
    </div>
  </div>
</div>
<div class="container">
  <div class="row">
    <%= form_with model: [@member, @report] do |f| %>
      <table class="mt-3 ml-5">
        <tr height="50">
          <th><%= f.label :通報理由 %></th>
          <td><%= f.text_area :reason, :size => "80x5", placeholder: "通報理由は、なるべく詳細にご記入ください。" %></td>
        </tr>
        <tr height="50">
          <th><%= f.label :当該ページ（URL等） %></th>
          <td><%= f.text_field :url, :size => "80x5", placeholder: "URL等が不明な場合は、空欄でも構いません。" %></td>
        </tr>
        <tr height="100">
          <th></th>
          <td><%= f.submit "通報する", class:"btn btn-danger mt-3 mb-5" %></td>
        </tr>
      </table>
    <% end %>
  </div>
</div>